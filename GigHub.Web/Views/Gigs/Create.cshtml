@model GigHub.DataLayer.Models.Gig
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>
<div class="container">
    <form>
        <div class="form-group">
            @Html.LabelFor(m => m.Venue)
            @Html.TextBoxFor(m => m.Venue, new { @class = "form-control" } )
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.DateTime)
        </div>
        <div class="form-group"></div>
        <div class="form-group"></div>
        <div class="form-group"></div>
    </form>
</div>

